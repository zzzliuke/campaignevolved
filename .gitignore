# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# tanstack start / vite / nitro
/.next/
/.output/
/.nitro/
/.tanstack/
/dist/
src/routeTree.gen.ts
src/paraglide/

# cloudflare — production resource IDs and routes are committed for CI;
# secrets stay in Cloudflare Worker secrets and are never stored here
/.wrangler/

# claude code local state
.claude/scheduled_tasks.lock

# production
/build

# deploy
.vercel

# editors (keep .cursor/skills — cross-agent symlink to .claude/skills)
.idea
.vscode
.cursor/*
!.cursor/skills

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env — local files hold secrets, never commit (keep .env.example tracked)
.env
.env.*
!.env.example

# typescript
*.tsbuildinfo

# database
/data/
src/config/db/schema.ts

# npm (project uses pnpm)
package-lock.json

# Runtime image uploads (no-storage local fallback)
/public/uploads/
